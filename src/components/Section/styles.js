import styled from 'styled-components';

export const Container = styled.div`
    padding: 24px 0 0 24px;
    margin-bottom: 24px;

    > h2 {
        font-size: 1.8rem;
        font-weight: 500;
        margin-bottom: 18px;
    }

    > div {
        display: flex;
        gap: 16px;
        
        overflow: hidden;
    }
`